"""Setup configuration for iqc-dashboard package."""

from setuptools import setup

# This minimal setup.py allows editable installs with older pip versions
# All package metadata is defined in pyproject.toml
setup()

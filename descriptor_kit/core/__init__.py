"""Frozen primitives for the descriptor kit: parsing, atom identification, and
the geometry/electronic/steric machinery the descriptor functions call.

Nothing here computes a named descriptor — that lives in ``descriptor_kit.descriptors``.
"""
